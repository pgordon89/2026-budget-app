{
  "name": "fiscus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An AI-native personal finance platform. Deterministic where money is involved, probabilistic where judgement is.",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate:data": "tsx scripts/generate-dataset.ts",
    "analyze:normalizer": "tsx scripts/analyze-normalizer.ts",
    "analyze:memory": "tsx scripts/analyze-memory.ts",
    "analyze:knn": "tsx scripts/analyze-knn.ts",
    "analyze:llm": "tsx scripts/analyze-llm.ts",
    "analyze:learning": "tsx scripts/analyze-learning.ts",
    "eval": "tsx evals/runner.ts",
    "test": "tsx scripts/test.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "@electric-sql/pglite": "^0.5.5",
    "drizzle-orm": "^0.45.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
